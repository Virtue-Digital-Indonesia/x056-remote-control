const paths={search:'<circle cx="10.5" cy="10.5" r="6.5"/><path d="m16 16 5 5"/>',plus:'<path d="M12 5v14M5 12h14"/>',chat:'<path d="M21 11a8 8 0 0 1-8 8H6l-4 3 1.5-6A8 8 0 1 1 21 11Z"/>',grid:'<rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/>',folder:'<path d="M3 7V5a1 1 0 0 1 1-1h5l2 3h9a1 1 0 0 1 1 1v11H3Z"/>',settings:'<circle cx="12" cy="12" r="3"/><path d="m9 3-1 3-3 1-2 3 2 3v4l3 2 4-1 3 2 3-2v-4l2-3-2-3-3-1-1-3Z"/>',arrow:'<path d="M12 19V5m-6 6 6-6 6 6"/>',chevron:'<path d="m9 5 7 7-7 7"/>',down:'<path d="m6 9 6 6 6-6"/>',attach:'<path d="m8 12 6-6a3 3 0 0 1 4 4l-8 8a5 5 0 0 1-7-7l9-9m-2 14 8-8"/>',moon:'<path d="M20 14A8.5 8.5 0 0 1 10 3 9 9 0 1 0 20 14Z"/>',sun:'<circle cx="12" cy="12" r="4"/><path d="M12 2v2m0 16v2M2 12h2m16 0h2M5 5l1 1m12 12 1 1M5 19l1-1M18 6l1-1"/>',clock:'<circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 2"/>',split:'<rect x="3" y="4" width="18" height="16" rx="2"/><path d="M12 4v16"/>',panel:'<rect x="3" y="4" width="18" height="16" rx="2"/><path d="M15 4v16"/>',close:'<path d="m6 6 12 12M6 18 18 6"/>',terminal:'<path d="m4 6 5 5-5 5m8 1h8"/>',file:'<path d="M14 3H5v18h14V8Zm0 0v5h5M8 13h8m-8 4h5"/>',check:'<path d="m5 12 4 4L19 6"/>',copy:'<rect x="8" y="8" width="12" height="13" rx="2"/><path d="M15 8V3H3v13h5"/>',branch:'<circle cx="6" cy="5" r="2"/><circle cx="6" cy="19" r="2"/><circle cx="18" cy="5" r="2"/><path d="M6 7v10m12-10c0 6-12 3-12 8"/>',more:'<circle cx="5" cy="12" r="1"/><circle cx="12" cy="12" r="1"/><circle cx="19" cy="12" r="1"/>',bell:'<path d="M18 8a6 6 0 0 0-12 0c0 8-3 8-3 9h18c0-1-3-1-3-9m-11 12h4"/>',play:'<path d="m8 4 12 8-12 8Z"/>',list:'<path d="M8 6h13M8 12h13M8 18h13M3 6h1m-1 6h1m-1 6h1"/>',bolt:'<path d="m13 2-9 12h7l-1 8 10-13h-7Z"/>',expand:'<path d="M14 3h7v7m0-7-7 7M10 21H3v-7m0 7 7-7"/>',stop:'<rect x="5" y="5" width="14" height="14" rx="2"/>',menu:'<path d="M4 6h16M4 12h16M4 18h16"/>',help:'<circle cx="12" cy="12" r="9"/><path d="M9 8a3 3 0 0 1 6 0c0 2-3 2-3 5m0 3h.01"/>'};
const icon=(name)=>`<svg viewBox="0 0 24 24" aria-hidden="true">${paths[name]||paths.chat}</svg>`;
const btn=(name,label,action,cls='')=>`<button class="icbtn ${cls}" title="${label}" aria-label="${label}" data-action="${action}">${icon(name)}</button>`;
const view=document.body.dataset.concept;
const combined=document.body.dataset.variant==='combined';
const bar=`<div class="studybar"><a class="studybrand" href="./">x056 / studies</a><nav aria-label="Design concepts">${['focus','workspace','control'].map((v,i)=>`<a href="${v}.html" ${view===v?'class="active" aria-current="page"':''}>0${i+1} ${v==='control'?'Control room':v[0].toUpperCase()+v.slice(1)}</a>`).join('')}</nav><span class="sample">INTERACTIVE DRAFT · SAMPLE DATA</span></div>`;
const actions=`<div class="message-actions">${btn('copy','Copy response','copy')}${btn('more','About this draft','about')}</div>`;
function assistant(text,provider='Astra',time='10:42',opts=''){return `<article class="message assistant"><div class="byline">${view==='focus'?'<span class="agent-mark">A</span>':'<span class="avatar">A</span>'}${provider}<span class="pill">${opts||'GPT-6'}</span><time class="spacer">${time}</time></div><div class="text">${text}</div>${actions}</article>`;}
function user(text){return `<article class="message user"><div class="byline">You <time>10:41</time></div><div class="text">${text}</div></article>`;}
const process=`<details class="process"><summary>${icon('check')} Read 4 files <span>·</span> Reviewed current layout <span>·</span> 12s</summary><div>✓ panel.html<br>✓ app.module.ts<br>✓ keyboard shortcuts<br>✓ mobile breakpoints</div></details>`;
function composer(label='Message Astra…'){return `<form class="composer"><textarea aria-label="Message" placeholder="${label}" rows="2"></textarea><div class="controls">${btn('attach','Attach a sample file','attach')}<select aria-label="Model"><option>Astra · GPT-6</option>Claude · Opus</option>GPT-5.6</option></select><select aria-label="Effort" class="effort"><option>High</option><option>Medium</option><option>Low</option></select><button class="send" type="submit" aria-label="Send demo message" title="Send demo message">${icon('arrow')}</button></div></form><p class="hint">Interactive draft · Enter to send · Shift+Enter for a new line</p>`;}
function rail(){return `<nav class="rail" aria-label="Workspace navigation"><a class="logo" href="./" title="All designs">x056</a>${btn('chat','Conversations','drawer','selected')}${btn('search','Search conversations','drawer')}${btn('plus','New conversation','new')}${btn('folder','Projects','drawer')}<span class="spacer"></span>${view==='focus'?btn('moon','Toggle light and dark','theme'):btn('help','About this draft','about')}<button class="avatar" data-action="about" title="Sample account" aria-label="Sample account">E</button></nav>`;}
const drawer=`<div class="veil" id="drawerVeil" hidden><aside class="drawer" role="dialog" aria-modal="true" aria-label="Conversations"><div class="row between"><span class="logo">x056</span>${btn('close','Close conversations','close-drawer')}</div><h2>Your conversations</h2><input id="drawerSearch" aria-label="Search conversations" placeholder="Find a conversation…"><div class="label">x056 remote control</div><button class="chatlink" data-chat="Redesign the panel">Redesign the panel<small>Astra · 2 minutes ago</small></button><button class="chatlink" data-chat="Keyboard navigation">Keyboard navigation<small>Claude · 35 minutes ago</small></button><div class="label">Customer portal</div><button class="chatlink" data-chat="API response handling">API response handling<small>Astra · Running</small></button><button class="chatlink" data-chat="Refresh the onboarding flow">Refresh the onboarding flow<small>Claude · Yesterday</small></button></aside></div>`;
const focusBody=`<div class="app"><div class="shell">${rail()}<main class="main"><header class="topline"><span class="crumb">x056 remote control</span><span class="crumb">/</span><span id="focusTitle">Redesign the panel</span><span class="spacer"></span><span class="pill"><i class="status"></i> Connected</span>${btn('more','About Focus','about')}</header><div class="scroll" id="focusScroll"><div class="conversation" id="focusThread"><div class="intro"><span class="label">Today</span><h1>Redesign the panel</h1><p>x056 remote control <span>·</span> Today, 10:41</p></div>${user('Let’s rethink this panel. I want a calmer place to work, with the conversation front and center.')}${assistant(`<p>Let’s give the conversation room to breathe.</p><p>The navigation can stay close without taking up a full column. Search brings any project or conversation one keystroke away.</p>${process}<h3>A simpler place to work</h3><p>A single rail for navigation. A generous reading area. A composer that keeps the model, attachments, and your next thought together.</p><p>Open the conversation drawer on the left, or try the controls below.</p>`)}</div></div><div class="composer-area"><div class="suggestions"><button data-prompt="Show me the mobile layout">Explore mobile</button><button data-prompt="Explain the navigation choices">Explain the navigation</button><button data-prompt="Try a different type scale">Try another type scale</button></div>${composer()}</div></main></div></div>`;
const workspaceChat=`${user('Can you simplify the panel layout and check that the composer still works on mobile?')}${assistant(`<p>I’ve reviewed the layout. The main issue is that navigation, conversation, and activity compete for the same space.</p><h3>Separate the workspace into three areas</h3><ul><li><strong>Projects and conversations</strong> stay in the sidebar.</li><li><strong>The active conversation</strong> gets its own tab.</li><li><strong>Activity and files</strong> move into the inspector.</li></ul>${process}<div class="session-note">Try Split view to keep another conversation open beside this one.</div>`,'Astra','10:42')}`;
const secondaryChat=`${user('Handle empty API responses without showing an error.')}${assistant(`<p>I added an explicit empty state before rendering the results.</p><div class="diff"><div class="dim">src/components/Results.tsx</div><div class="remove">− if (!data) return &lt;Error /&gt;;</div><div class="add">+ if (!data?.length) {</div><div class="add">+   return &lt;EmptyState /&gt;;</div><div class="add">+ }</div></div><p style="margin-top:14px">The request can now succeed with no results. The empty state explains what to try next.</p>`,'Astra','10:44')}`;
const workspaceBody=`<div class="app"><div class="shell">${rail()}<aside class="sidebar"><div class="row between"><h2>Workspace</h2>${btn('search','Search workspace','drawer')}</div><div class="side-section"><div class="label">Projects</div><button class="side-item" data-action="drawer">${icon('down')}${icon('folder')}x056 remote control</button><button class="side-item indent current" data-tab="panel">Redesign the panel<i class="status busy"></i></button><button class="side-item indent" data-tab="keyboard">Keyboard navigation</button><button class="side-item" data-action="drawer">${icon('chevron')}${icon('folder')}Customer portal</button><button class="side-item" data-action="drawer">${icon('chevron')}${icon('folder')}Reporting API</button></div><div class="side-section"><div class="label">Running elsewhere</div><button class="side-item" data-tab="api"><i class="status busy"></i>API response handling</button><button class="side-item" data-action="drawer"><i class="status"></i>Update API docs</button></div><div class="spacer"></div><div class="branch">${icon('branch')} ui/panel-refresh</div><div class="row" style="padding:8px"><span class="avatar">E</span><div style="font-size:11px">Efran<div class="muted" style="font-size:9px">Personal workspace</div></div></div></aside><main class="main"><header class="topline"><span class="muted">x056 remote control</span><span class="muted">/</span><span>Workspace</span><span class="spacer"></span><button class="outline" data-action="split" id="splitBtn" aria-pressed="false" style="font-size:10px;padding:4px 9px">${icon('split')} Split view</button>${btn('panel','Toggle activity inspector','inspector','desktop-only')}</header><nav class="tabs" aria-label="Open conversations"><button class="tab active" data-tab="panel"><i class="status busy"></i>Redesign the panel</button><button class="tab" data-tab="api">API response handling</button><button class="tab" data-tab="keyboard">Keyboard navigation</button>${btn('plus','New conversation','new')}</nav><div class="workbody"><div class="chat-panes"><section class="chat-pane"><div class="pane-title"><i class="status busy"></i><span id="workTitle">Redesign the panel</span><span class="spacer"></span><span>GPT-6 · High</span></div><div class="scroll"><div class="conversation" id="workThread">${workspaceChat}</div></div><div class="composer-area">${composer()}</div></section><section class="chat-pane secondary" hidden><div class="pane-title">Customer portal<span class="spacer"></span>${btn('close','Close split view','split')}</div><div class="scroll"><div class="conversation">${secondaryChat}</div></div><div class="composer-area">${composer('Message about the API…')}</div></section></div><div class="resize-handle" role="separator" aria-label="Resize activity inspector" aria-orientation="vertical" aria-valuemin="210" aria-valuemax="400" aria-valuenow="268" tabindex="0"></div><aside class="inspect"><header class="inspector-head"><span>Activity</span><span class="pill"><i class="status busy"></i> Working</span></header><section class="inspector-section"><div class="label">This turn</div><div class="activity-line">Read layout and styles<small>4 files · 12 seconds</small></div><div class="activity-line">Updated navigation<small>panel.html · 8 changes</small></div><div class="activity-line">Checked keyboard focus<small>All controls reachable</small></div><div class="activity-line live">Reviewing mobile layout<small>390 × 844 viewport</small></div></section><section class="inspector-section"><div class="label">Changed files · 3</div><div class="file-row">${icon('file')}panel.html<span class="plus">+48</span><span class="minus">−21</span></div><div class="file-row">${icon('file')}styles.css<span class="plus">+126</span><span class="minus">−34</span></div><div class="file-row">${icon('file')}navigation.js<span class="plus">+17</span><span class="minus">−8</span></div><button class="outline" data-action="diff" style="width:100%;font-size:10px;margin-top:10px;justify-content:center">Review changes ${icon('chevron')}</button></section><section class="inspector-section"><div class="label">Session</div><div class="row between" style="font-size:10px"><span class="muted">Context used</span><span>38%</span></div><div class="meter"><i></i></div><div class="row between" style="font-size:10px;margin-top:15px"><span class="muted">Elapsed</span><span class="mono">2m 14s</span></div><div class="row between" style="font-size:10px;margin-top:9px"><span class="muted">Account</span><span>Personal · ChatGPT</span></div></section></aside></div><footer class="footer"><span>${icon('branch')} ui/panel-refresh</span><span><i class="status"></i> Connected</span><span class="spacer"></span><span>3 files changed</span><span class="desktop-only">Sample session</span></footer></main></div></div>`;
const tasks=[{id:'panel',project:'x056 remote control',title:'Redesign the panel',desc:'Refining navigation, message layout, and mobile controls.',action:'Reviewing mobile layout',state:'running',agent:'Astra',model:'GPT-6',time:'2m 14s'}, {id:'api',project:'Customer portal',title:'Handle empty API responses',desc:'Give successful requests with no results a useful empty state.',action:'Running component checks',state:'running',agent:'Astra',model:'GPT-6',time:'5m 08s'}, {id:'auth',project:'Customer portal',title:'Refresh the onboarding flow',desc:'The new flow is ready. One design decision needs your input.',action:'Choose the welcome screen',state:'waiting',agent:'Claude',model:'Opus',time:'Waiting 3m'}, {id:'report',project:'Reporting API',title:'Add a weekly export',desc:'The export is ready. Confirm when the weekly job should run.',action:'Choose a schedule',state:'waiting',agent:'Astra',model:'GPT-6',time:'Waiting 8m'}, {id:'docs',project:'Documentation',title:'Update API docs',desc:'Added request examples and documented the new response fields.',action:'8 pages updated',state:'done',agent:'Claude',model:'Opus',time:'10 min ago'}, {id:'test',project:'x056 remote control',title:'Fix keyboard navigation',desc:'Project search, tab focus, and Escape now work together.',action:'12 checks passed',state:'done',agent:'Astra',model:'GPT-6',time:'24 min ago'}];
const controlBody=`<div class="app"><div class="control-shell"><header class="control-top"><a class="logo" href="./">x056</a><div class="sep"></div><span style="font-size:12px">Control room</span><span class="spacer"></span><span class="pill"><i class="status"></i> All systems connected</span>${btn('bell','View items needing input','attention','desktop-only')}${btn('settings','Chat display settings','preferences')}<button class="avatar" data-action="accounts" title="Accounts and usage" aria-label="Accounts and usage">E</button></header><div class="body"><nav class="control-nav" aria-label="Board navigation"><button class="navlink active" data-filter="all">${icon('grid')}Overview</button><button class="navlink" data-filter="waiting">${icon('chat')}Needs input<b id="navWaiting">2</b></button><button class="navlink" data-filter="done">${icon('check')}Completed</button><button class="navlink" data-action="accounts">${icon('settings')}Accounts</button><div class="label">Projects</div><button class="navlink" data-project="x056"><i class="project-dot"></i>x056 remote control</button><button class="navlink" data-project="Customer"><i class="project-dot" style="background:#b4a0cf"></i>Customer portal</button><button class="navlink" data-project="Reporting"><i class="project-dot" style="background:#8aaf99"></i>Reporting API</button><button class="navlink" data-project="Documentation"><i class="project-dot" style="background:#c8ab7b"></i>Documentation</button><div class="spacer"></div><button class="navlink" data-action="about">${icon('help')}About this draft</button></nav><main class="boardmain"><header class="board-heading"><div><span class="label">Monday, September 7</span><h1>Your agents, at a glance.</h1><p id="boardSummary">2 working. 2 need your input. Everything in one place.</p></div><button class="primary" data-action="new-task">${icon('plus')}<span>New task</span></button></header><div class="stats"><div class="stat"><div><strong id="statRunning">2</strong><p>Running now</p></div><span class="stat-icon">${icon('bolt')}</span></div><div class="stat wait"><div><strong id="statWaiting">2</strong><p>Need your input</p></div><span class="stat-icon">${icon('chat')}</span></div><div class="stat done"><div><strong>2</strong><p>Finished today</p></div><span class="stat-icon">${icon('check')}</span></div><div class="stat"><div><strong>4</strong><p>Active projects</p></div><span class="stat-icon">${icon('folder')}</span></div></div><div class="board-tools"><div class="filtertabs" aria-label="Filter tasks"><button class="active" data-filter="all">All tasks</button><button data-filter="running">Running</button><button data-filter="waiting">Needs input</button><button data-filter="done">Finished</button></div><label class="board-search">${icon('search')}<input id="taskSearch" placeholder="Find a task…" aria-label="Find a task"></label><div class="view-switch">${btn('grid','Board view','board','active')}${btn('list','List view','list')}</div></div><div class="board" id="board"></div><div class="notice">${icon('clock')}Sample activity · Open a card to read the conversation or respond to a question.</div></main></div></div></div><div class="detail-veil" id="detailVeil" hidden><aside class="detail" role="dialog" aria-modal="true" aria-label="Task conversation"><nav class="focus-rail rail" aria-label="Focus navigation"><button class="logo" data-action="close-detail" title="Back to Control room" aria-label="Back to Control room">x056</button>${btn('grid','Back to Control room','close-detail')}${btn('chat','Switch conversation','drawer','selected')}${btn('search','Search conversations','drawer')}<span class="spacer"></span>${btn('settings','Chat display settings','preferences')}${btn('moon','Toggle Focus theme','focus-theme')}<button class="avatar" data-action="accounts" title="Accounts and usage" aria-label="Accounts and usage">E</button></nav><main class="detail-main"><header><div class="row"><span class="label" id="detailProject"></span><span class="spacer"></span>${btn('settings','Chat display settings','preferences','detail-settings')}${btn('moon','Toggle Focus theme','focus-theme','modal-theme')}${btn('expand','Maximize into Focus','expand')}${btn('close','Close conversation','close-detail')}</div><h2 id="detailTitle"></h2><span class="pill" id="detailStatus"></span></header><div class="scroll"><div class="conversation" id="detailThread"></div></div><div class="composer-area">${composer('Reply to this task…')}</div></main></aside></div>`;
const combinedBar=`<div class="studybar"><a class="studybrand" href="./">x056 / studies</a><span class="combined-name">Control room + Focus</span><span class="sample">INTERACTIVE DRAFT · SAMPLE DATA</span></div>`;
document.body.innerHTML=(combined?combinedBar:bar)+(view==='focus'?focusBody:view==='workspace'?workspaceBody:controlBody)+drawer+`<div class="veil" id="newTaskVeil" hidden><aside class="drawer" role="dialog" aria-modal="true" aria-label="New sample task"><div class="row between"><h2>New task</h2>${btn('close','Close new task','close-new-task')}</div><form class="task-form" id="newTaskForm"><p>Add a task to this sample board.</p><label for="taskName">What should the agent work on?</label><input id="taskName" required maxlength="90" placeholder="e.g. Review the login flow"><label for="taskProject">Project</label><select id="taskProject"><option>x056 remote control</option><option>Customer portal</option><option>Reporting API</option><option>Documentation</option></select><button class="primary" type="submit">Start sample task</button></form></aside></div>`+'<div class="toast" role="status" id="toast" hidden></div>';
document.querySelectorAll('.composer button:not(.send)').forEach(b=>b.type='button');
let lastFocus=null,toastTimer,selectedTask=null,filter='all';
let taskTrigger=null;
const taskViews=new Map();
const PREFS_KEY='x056_draft_chat_preferences_v1';
let chatPrefs={open:'side',maximize:'page'};
try {const saved=JSON.parse(localStorage.getItem(PREFS_KEY)||'{}');if(['side','maximized'].includes(saved.open))chatPrefs.open=saved.open;if(['page','modal'].includes(saved.maximize))chatPrefs.maximize=saved.maximize;} catch {}
let preferencesTrigger=null;
document.body.insertAdjacentHTML('beforeend',`<div class="preferences-veil" id="preferencesVeil" hidden><section class="preferences" role="dialog" aria-modal="true" aria-labelledby="preferencesTitle"><header class="row between"><h2 id="preferencesTitle">Chat display</h2>${btn('close','Close display settings','close-preferences')}</header><p>Choose how conversations open in this preview.</p><fieldset><legend>When you open a task</legend><div class="preference-options"><label><input type="radio" name="openChat" value="side"><span>${icon('panel')}<strong>Side panel</strong><small>Keep the board in view</small></span></label><label><input type="radio" name="openChat" value="maximized"><span>${icon('expand')}<strong>Maximized</strong><small>Go straight into Focus</small></span></label></div></fieldset><fieldset><legend>When the chat is maximized</legend><div class="preference-options"><label><input type="radio" name="maximizeChat" value="page"><span>${icon('expand')}<strong>Full page</strong><small>Fill the app window</small></span></label><label><input type="radio" name="maximizeChat" value="modal"><span>${icon('chat')}<strong>Large modal</strong><small>Float above the board</small></span></label></div></fieldset><footer><span id="preferencesNote">Saved automatically in this browser.</span><button class="primary" data-action="close-preferences">Done</button></footer></section></div>`);
function openPreferences(){
 preferencesTrigger=document.activeElement;
 document.querySelector('input[name="openChat"][value="'+chatPrefs.open+'"]').checked=true;
 document.querySelector('input[name="maximizeChat"][value="'+chatPrefs.maximize+'"]').checked=true;
 document.getElementById('preferencesVeil').hidden=false;
 document.querySelector('.app').inert=true;
 if(view==='control')document.querySelector('.detail').inert=true;
 document.querySelector('.studybar').inert=true;
 document.querySelector('#preferencesVeil input:checked').focus();
}
function closePreferences(){
 document.getElementById('preferencesVeil').hidden=true;
 document.querySelector('.app').inert=view==='control'&&!document.getElementById('detailVeil').hidden;
 if(view==='control')document.querySelector('.detail').inert=false;
 document.querySelector('.studybar').inert=false;
 preferencesTrigger?.focus();
}
document.getElementById('preferencesVeil').addEventListener('click',e=>{if(e.target.id==='preferencesVeil')closePreferences();});
document.getElementById('preferencesVeil').addEventListener('change',e=>{
 if(e.target.name==='openChat')chatPrefs.open=e.target.value;
 if(e.target.name==='maximizeChat')chatPrefs.maximize=e.target.value;
 try {localStorage.setItem(PREFS_KEY,JSON.stringify(chatPrefs));document.getElementById('preferencesNote').textContent='Saved automatically in this browser.';} catch {document.getElementById('preferencesNote').textContent='Applied for this visit. Browser storage is unavailable.';}
 if(view==='control'&&document.querySelector('.detail').classList.contains('fullscreen'))setFocusMode(true);
});
function toast(message){const el=document.getElementById('toast');el.textContent=message;el.hidden=false;clearTimeout(toastTimer);toastTimer=setTimeout(()=>el.hidden=true,3500);}
function openDrawer(){
 lastFocus=document.activeElement;
 if(view==='control'){
  const drawer=document.querySelector('#drawerVeil .drawer');
  drawer.querySelectorAll('.chatlink,.label').forEach(el=>el.remove());
  tasks.forEach(t=>{const button=document.createElement('button');button.className='chatlink';button.dataset.task=t.id;button.append(document.createTextNode(t.title));const subtitle=document.createElement('small');subtitle.textContent=t.project+' · '+t.agent;button.append(subtitle);drawer.append(button);});
 }
 document.getElementById('drawerSearch').value='';
 document.querySelectorAll('.chatlink').forEach(el=>el.hidden=false);
 document.getElementById('drawerVeil').hidden=false;
 if(view==='control')document.querySelector('.detail').inert=true;
 document.getElementById('drawerSearch').focus();
}
function closeDrawer(){document.getElementById('drawerVeil').hidden=true;if(view==='control')document.querySelector('.detail').inert=false;lastFocus?.focus();}
function saveTaskView(){
 if(!selectedTask||document.getElementById('detailVeil').hidden)return;
 const detail=document.querySelector('.detail');
 taskViews.set(selectedTask.id,{nodes:Array.from(document.getElementById('detailThread').childNodes),draft:detail.querySelector('textarea').value,model:detail.querySelector('select[aria-label="Model"]').selectedIndex,effort:detail.querySelector('select[aria-label="Effort"]').selectedIndex,attachment:detail.querySelector('.attachment'),scroll:detail.querySelector('.scroll').scrollTop});
}
function closeDetail(){
 saveTaskView();
 document.getElementById('detailVeil').hidden=true;
 document.querySelector('.app').inert=false;
 setFocusMode(false);
 // Board rendering may have replaced the card while a decision was answered.
 const card=tasks.find(t=>t.id===selectedTask?.id)&&Array.from(document.querySelectorAll('.taskcard')).find(el=>el.dataset.task===selectedTask.id);
 (card||taskTrigger||document.getElementById('taskSearch'))?.focus();
}
function setFocusMode(expanded,preservePosition=true){
 const detail=document.querySelector('.detail'),scroll=detail.querySelector('.scroll');
 const children=Array.from(document.getElementById('detailThread').children).filter(el=>el.getClientRects().length);
 const top=scroll.getBoundingClientRect().top;
 const anchor=children.find(el=>el.getBoundingClientRect().bottom>top);
 const offset=anchor?anchor.getBoundingClientRect().top-top:0;
 const bottom=scroll.scrollHeight-scroll.scrollTop-scroll.clientHeight<5;
 detail.classList.toggle('fullscreen',expanded);
 document.getElementById('detailVeil').classList.toggle('focused',expanded);
 document.getElementById('detailVeil').classList.toggle('modal-mode',expanded&&chatPrefs.maximize==='modal');
 detail.classList.toggle('big-modal',expanded&&chatPrefs.maximize==='modal');
 const button=detail.querySelector('[data-action="expand"]');
 const label=expanded?'Restore side panel':'Maximize into Focus';
 button.title=label;button.setAttribute('aria-label',label);button.setAttribute('aria-pressed',String(expanded));
 button.innerHTML=expanded?icon('panel'):icon('expand');
 requestAnimationFrame(()=>{if(!preservePosition)return;if(bottom)scroll.scrollTop=scroll.scrollHeight;else if(anchor)scroll.scrollTop+=anchor.getBoundingClientRect().top-scroll.getBoundingClientRect().top-offset;});
}
function applyTab(key){const names={panel:'Redesign the panel',api:'API response handling',keyboard:'Keyboard navigation'};document.getElementById('workTitle').textContent=names[key];document.getElementById('workThread').innerHTML=key==='panel'?workspaceChat:key==='api'?secondaryChat:assistant('<p>Keyboard navigation now follows the same order as the screen.</p><ul><li>Tab moves between controls.</li><li>Escape closes drawers and dialogs.</li><li>Search filters projects and conversations.</li></ul><div class="session-note">All 12 checks passed in this sample session.</div>');document.querySelectorAll('.tab').forEach(t=>{t.classList.toggle('active',t.dataset.tab===key);t.setAttribute('aria-pressed',String(t.dataset.tab===key));});}
const escapeHTML=text=>String(text).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function taskCard(t){return `<button class="taskcard ${t.state==='waiting'?'wait':t.state==='done'?'done':''}" data-task="${t.id}" aria-label="Open ${escapeHTML(t.title)}"><div class="project"><i class="project-dot"></i>${t.project}</div><h3>${escapeHTML(t.title)}</h3><p class="desc">${escapeHTML(t.desc)}</p><div class="preview">${icon(t.state==='waiting'?'chat':t.state==='done'?'check':'terminal')}${t.action}</div><div class="bottom"><span class="avatar">${t.agent[0]}</span>${t.agent}<span>· ${t.model}</span><time>${t.time}</time></div></button>`;}
function renderBoard(){const q=document.getElementById('taskSearch').value.toLowerCase();const board=document.getElementById('board');let count=0;board.innerHTML=['running','waiting','done'].filter(s=>filter==='all'||filter===s).map(s=>{const ts=tasks.filter(t=>t.state===s&&(t.project+' '+t.title).toLowerCase().includes(q));count+=ts.length;return `<section class="lane"><div class="lane-head"><i class="status ${s==='running'?'busy':s==='waiting'?'wait':''}"></i>${s==='waiting'?'Needs input':s==='running'?'Running':'Finished'}<span class="count">${ts.length}</span></div>${ts.map(taskCard).join('')}${ts.length?'':'<p class="muted" style="font-size:11px;padding:10px 0">No matching tasks</p>'}</section>`;}).join('');document.querySelectorAll('[data-filter]').forEach(b=>b.classList.toggle('active',b.dataset.filter===filter));document.getElementById('statRunning').textContent=tasks.filter(t=>t.state==='running').length;document.getElementById('statWaiting').textContent=tasks.filter(t=>t.state==='waiting').length;document.getElementById('navWaiting').textContent=tasks.filter(t=>t.state==='waiting').length;document.getElementById('boardSummary').textContent=`${tasks.filter(t=>t.state==='running').length} working. ${tasks.filter(t=>t.state==='waiting').length} need your input. Everything in one place.`;}
function openTask(id){const t=tasks.find(t=>t.id===id);if(!t)return;saveTaskView();const firstClick=document.getElementById('detailVeil').hidden;if(firstClick)taskTrigger=document.activeElement;selectedTask=t;lastFocus=document.activeElement;document.getElementById('detailProject').textContent=t.project;document.getElementById('detailTitle').textContent=t.title;document.getElementById('detailStatus').textContent=t.state==='waiting'?'Needs your input':t.state==='running'?'Working':'Finished';const decision=t.state==='waiting'?`<div class="decision"><p>${id==='auth'?'Which welcome screen should I use for the onboarding flow?':'When should the weekly export run?'}</p><div class="row"><button class="primary" data-action="decide">${id==='auth'?'Use the simple welcome':'Monday at 09:00'}</button><button class="outline" data-action="decide">${id==='auth'?'Show the product tour':'Friday at 17:00'}</button></div></div>`:'';document.getElementById('detailThread').innerHTML=user(escapeHTML(t.desc))+assistant(`<p>${t.state==='waiting'?'The implementation is ready for the next step. I need your preference before continuing.':t.state==='done'?'This task is complete. Here is the result.':'I’m working through this task. Here is the latest activity.'}</p><details class="process" open><summary>${icon(t.state==='done'?'check':'terminal')}${t.action}</summary><div>${t.state==='done'?'✓ Updated the relevant files<br>✓ Checked the result<br>✓ Ready for review':'✓ Reviewed the existing implementation<br>✓ Prepared the changes<br>→ '+t.action}</div></details>${decision}`,t.agent,'10:44',t.model);const thread=document.getElementById('detailThread');
if(id==='panel')thread.innerHTML=user('Use Control room as the home screen, with Focus when I maximize a conversation.')+assistant('<p>The board gives you an overview of every agent. Open a task to read or reply without leaving that overview.</p><h3>One conversation, two views</h3><p>Maximize the chat for a quiet reading area, a narrow navigation rail, and a centered composer. Restore brings the same conversation back beside the board.</p><details class="process"><summary>'+icon('check')+'Conversation state stays with you</summary><div>Messages and pending questions<br>Your unsent reply and model selection<br>Attachments and reading position</div></details><p>Try typing a reply, maximizing the chat, and restoring it. Your draft stays in place.</p>','Astra','10:44');
const saved=taskViews.get(id);
if(saved)thread.replaceChildren(...saved.nodes);
else {
 const intro=document.createElement('div');intro.className='focus-intro';
 const eyebrow=document.createElement('span');eyebrow.className='label';eyebrow.textContent='Today';
 const title=document.createElement('h1');title.textContent=t.title;
 const context=document.createElement('p');context.textContent=t.project+' · '+t.agent;
 intro.append(eyebrow,title,context);thread.prepend(intro);
}
const detail=document.querySelector('.detail');
detail.querySelector('textarea').value=saved?.draft||'';
detail.querySelector('textarea').placeholder='Message '+t.agent+'…';
detail.querySelector('select[aria-label="Model"]').selectedIndex=saved?saved.model:(t.agent==='Claude'?1:0);
detail.querySelector('select[aria-label="Effort"]').selectedIndex=saved?.effort||0;
detail.querySelector('.attachment')?.remove();if(saved?.attachment)detail.querySelector('.composer').prepend(saved.attachment);
document.getElementById('detailVeil').hidden=false;
document.querySelector('.app').inert=true;
if(firstClick)setFocusMode(chatPrefs.open==='maximized',false);
detail.querySelector('.scroll').scrollTop=saved?.scroll||0;
detail.querySelector('[data-action="expand"]').focus();}
function setSplit(){document.body.classList.toggle('split');document.querySelector('.secondary').hidden=!document.body.classList.contains('split');document.getElementById('splitBtn').setAttribute('aria-pressed',String(document.body.classList.contains('split')));}
document.addEventListener('click',e=>{const el=e.target.closest('button,[data-prompt]');if(!el)return;if(el.dataset.prompt){document.querySelector('.composer textarea').value=el.dataset.prompt;document.querySelector('.composer textarea').focus();}if(el.dataset.tab)applyTab(el.dataset.tab);if(el.dataset.task){if(!document.getElementById('drawerVeil').hidden)closeDrawer();openTask(el.dataset.task);}if(el.dataset.filter){if(typeof showControlSection==='function')showControlSection('board');filter=el.dataset.filter;renderBoard();}if(el.dataset.project){if(typeof showControlSection==='function')showControlSection('board');document.getElementById('taskSearch').value=el.dataset.project;filter='all';renderBoard();}if(el.dataset.chat){closeDrawer();if(view==='workspace')applyTab(el.dataset.chat.includes('API')?'api':el.dataset.chat.includes('Keyboard')?'keyboard':'panel');else if(view==='focus'){document.getElementById('focusTitle').textContent=el.dataset.chat;document.querySelector('.intro h1').textContent=el.dataset.chat;toast('Sample conversation selected');}else{document.getElementById('taskSearch').value=el.dataset.chat.includes('API')?'API':'';renderBoard();}}const a=el.dataset.action;
if(a==='accounts'&&typeof openAccounts==='function')openAccounts();
if(a==='preferences')openPreferences();if(a==='close-preferences')closePreferences();
if(a==='drawer')openDrawer();if(a==='close-drawer')closeDrawer();if(a==='theme'){document.body.classList.toggle('dark');el.innerHTML=icon(document.body.classList.contains('dark')?'sun':'moon');el.setAttribute('aria-pressed',String(document.body.classList.contains('dark')));}
if(a==='new'){if(view==='focus'){document.getElementById('focusTitle').textContent='New conversation';document.getElementById('focusThread').innerHTML='<div class="intro"><span class="label">A fresh start</span><h1>What are we working on?</h1><p>Write your first message below.</p></div>';}else if(view==='workspace'){document.getElementById('workTitle').textContent='New conversation';document.getElementById('workThread').innerHTML=assistant('<p>What would you like to work on?</p>');document.querySelectorAll('.tab').forEach(t=>t.classList.remove('active'));}const input=document.querySelector('.composer textarea');input.value='';input.focus();}
if(a==='attach'){const c=el.closest('.composer');let note=c.querySelector('.attachment');if(!note){note=document.createElement('div');note.className='attachment pill';note.textContent='panel-reference.png · Sample attachment';note.style.marginBottom='10px';c.prepend(note);}else note.remove();}
if(a==='copy'){const text=el.closest('.message').querySelector('.text').innerText;navigator.clipboard?.writeText(text).then(()=>toast('Response copied')).catch(()=>toast('Clipboard unavailable in this preview'));}
if(a==='about')toast('Interactive design draft with sample data. Try navigation, search, and the composer.');
if(a==='split')setSplit();if(a==='inspector'){const panel=document.querySelector('.inspect');const mobile=innerWidth<=950;document.body.classList.remove('split');document.querySelector('.secondary').hidden=true;document.getElementById('splitBtn').setAttribute('aria-pressed','false');panel.hidden=!panel.hidden;document.querySelector('.resize-handle').hidden=panel.hidden;if(mobile)toast('The inspector is available in the wider desktop layout.');}
if(a==='diff'){applyTab('api');toast('Showing a sample change in the API conversation');}
if(a==='attention'){if(typeof showControlSection==='function')showControlSection('board');filter='waiting';renderBoard();}
if(a==='board'||a==='list'){document.getElementById('board').classList.toggle('list',a==='list');document.querySelectorAll('.view-switch button').forEach(b=>b.classList.toggle('active',b.dataset.action===a));}
if(a==='close-detail')closeDetail();if(a==='expand')setFocusMode(!document.querySelector('.detail').classList.contains('fullscreen'));
if(a==='focus-theme'){const detail=document.querySelector('.detail');detail.classList.toggle('dark');detail.querySelectorAll('[data-action="focus-theme"]').forEach(button=>{button.innerHTML=icon(detail.classList.contains('dark')?'sun':'moon');button.setAttribute('aria-pressed',String(detail.classList.contains('dark')));});}
if(a==='decide'){selectedTask.state='running';selectedTask.action='Applying your choice';selectedTask.time='Just now';renderBoard();document.getElementById('detailStatus').textContent='Working';el.closest('.decision').innerHTML='<p>Choice saved in this preview. The agent is continuing.</p>';toast('Sample task moved to Running');}
if(a==='new-task'){lastFocus=document.activeElement;document.getElementById('newTaskVeil').hidden=false;document.getElementById('taskName').focus();}if(a==='close-new-task'){document.getElementById('newTaskVeil').hidden=true;lastFocus?.focus();}
});
document.getElementById('drawerSearch').addEventListener('input',e=>document.querySelectorAll('.chatlink').forEach(b=>b.hidden=!b.textContent.toLowerCase().includes(e.target.value.toLowerCase())));
document.getElementById('drawerVeil').addEventListener('click',e=>{if(e.target.id==='drawerVeil')closeDrawer();});
if(view==='control'){document.getElementById('taskSearch').addEventListener('input',renderBoard);document.getElementById('detailVeil').addEventListener('click',e=>{if(e.target.id==='detailVeil')closeDetail();});renderBoard();}
document.addEventListener('keydown',e=>{if(e.key==='Escape'){if(!document.getElementById('preferencesVeil').hidden)closePreferences();else if(!document.getElementById('newTaskVeil').hidden){document.getElementById('newTaskVeil').hidden=true;lastFocus?.focus();}else if(!document.getElementById('drawerVeil').hidden)closeDrawer();else if(view==='control'&&!document.getElementById('detailVeil').hidden)closeDetail();}if((e.ctrlKey||e.metaKey)&&e.key==='k'){e.preventDefault();if(document.getElementById('preferencesVeil').hidden)openDrawer();}if(e.key==='Tab'){const modal=[...document.querySelectorAll('[role="dialog"]')].find(d=>d.parentElement.hidden===false&&!d.inert);if(modal){const items=[...modal.querySelectorAll('button,input,textarea,select,a[href]')].filter(n=>n.getClientRects().length&&!n.disabled);if(e.shiftKey&&document.activeElement===items[0]){e.preventDefault();items.at(-1).focus();}else if(!e.shiftKey&&document.activeElement===items.at(-1)){e.preventDefault();items[0].focus();}}}});
document.querySelectorAll('.composer').forEach(form=>{form.addEventListener('submit',e=>{e.preventDefault();const input=form.querySelector('textarea');const text=input.value.trim();if(!text)return;const pane=form.closest('.chat-pane')||form.closest('.detail')||document.querySelector('.main');const thread=pane.querySelector('.conversation');const msg=document.createElement('article');msg.className='message user';const content=document.createElement('div');content.className='text';content.textContent=text;msg.append(content);thread.append(msg);const reply=document.createElement('article');reply.className='message demo-result';reply.textContent='Message added to this draft. In the working panel, the agent’s response would stream here.';thread.append(reply);input.value='';pane.querySelector('.scroll').scrollTop=pane.querySelector('.scroll').scrollHeight;});form.querySelector('textarea').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey&&!e.isComposing&&!matchMedia('(pointer: coarse)').matches){e.preventDefault();form.requestSubmit();}});});
if(view==='workspace'){const handle=document.querySelector('.resize-handle'),panel=document.querySelector('.inspect');const size=w=>{const width=Math.min(400,Math.max(210,w));panel.style.width=width+'px';handle.setAttribute('aria-valuenow',width);};handle.addEventListener('pointerdown',e=>{handle.setPointerCapture(e.pointerId);const start=e.clientX,initial=panel.getBoundingClientRect().width;const move=ev=>size(initial+start-ev.clientX);handle.addEventListener('pointermove',move);handle.addEventListener('pointerup',()=>handle.removeEventListener('pointermove',move),{once:true});});handle.addEventListener('keydown',e=>{if(['ArrowLeft','ArrowRight'].includes(e.key)){e.preventDefault();size(panel.getBoundingClientRect().width+(e.key==='ArrowLeft'?10:-10));}});}

document.getElementById('newTaskVeil').addEventListener('click',e=>{if(e.target.id==='newTaskVeil'){e.target.hidden=true;lastFocus?.focus();}});
document.getElementById('newTaskForm').addEventListener('submit',e=>{e.preventDefault();const name=document.getElementById('taskName').value.trim();if(!name)return;tasks.push({id:'sample-'+Date.now(),project:document.getElementById('taskProject').value,title:name,desc:'A new task from your sample workspace.',action:'Reviewing the request',state:'running',agent:'Astra',model:'GPT-6',time:'Just now'});document.getElementById('newTaskVeil').hidden=true;document.getElementById('taskName').value='';document.getElementById('taskSearch').value='';filter='all';renderBoard();lastFocus?.focus();toast('Sample task added to Running');});
